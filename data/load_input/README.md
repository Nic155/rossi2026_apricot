# load_input

The deleterious-variant table (`result3-provean_forGL+score+class.C1_W2.subset.tsv`) is not stored in this repository due to its size (~1.3 GB). It is produced by the upstream genetic-load pipeline.

To reproduce analyses, download the file from the data deposit (Zenodo DOI: TODO) and place it in this folder.
